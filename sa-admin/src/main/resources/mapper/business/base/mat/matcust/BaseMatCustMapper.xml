<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.base.mat.matcust.dao.BaseMatCustDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        t_base_mat_cust.mat_cust_id,
        t_base_mat_cust.mat_id,
        t_base_mat_cust.cust_id,
        t_base_mat_cust.master,
        t_base_mat_cust.cust_mat_name,
        t_base_mat_cust.price,
        t_base_mat_cust.waste_percent,
        t_base_mat_cust.waste_num,
        t_base_mat_cust.remark,
        t_base_mat_cust.company_id,
        t_base_mat_cust.version,
        t_base_mat_cust.create_user_id,
        t_base_mat_cust.create_user_name,
        t_base_mat_cust.create_time,
        t_base_mat_cust.update_user_id,
        t_base_mat_cust.update_user_name,
        t_base_mat_cust.update_time,
        t_base_mat_cust.deleted_flag
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.base.mat.matcust.domain.vo.BaseMatCustVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_base_mat_cust
    </select>

    <select id="findListByMatId"
            resultType="net.lab1024.sa.admin.module.business.base.mat.matcust.domain.vo.BaseMatCustVO">
        SELECT
        <include refid="base_columns"/>,
        t_base_cust.cust_name
        FROM t_base_mat_cust t_base_mat_cust
        LEFT JOIN t_base_cust t_base_cust ON t_base_mat_cust.cust_id = t_base_cust.cust_id AND t_base_cust.deleted_flag
        = 0
        <where>
            t_base_mat_cust.mat_id = #{matId}
            AND t_base_mat_cust.deleted_flag = 0
        </where>
        ORDER BY t_base_mat_cust.create_time
    </select>


    <update id="batchUpdateDeleted">
        update t_base_mat_cust set deleted_flag = #{deletedFlag}
        where mat_cust_id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

    <update id="updateDeleted">
        update t_base_mat_cust
        set deleted_flag = #{deletedFlag}
        where mat_cust_id = #{matCustId}
    </update>

</mapper>
