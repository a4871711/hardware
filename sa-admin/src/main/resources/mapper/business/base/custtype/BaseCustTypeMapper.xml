<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.base.custtype.dao.BaseCustTypeDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        t_base_cust_type.cust_type_id,
        t_base_cust_type.cust_type_code,
        t_base_cust_type.cust_type_name,
        t_base_cust_type.parent_id,
        t_base_cust_type.remark,
        t_base_cust_type.company_id,
        t_base_cust_type.version,
        t_base_cust_type.create_user_id,
        t_base_cust_type.create_user_name,
        t_base_cust_type.create_time,
        t_base_cust_type.update_user_id,
        t_base_cust_type.update_user_name,
        t_base_cust_type.update_time,
        t_base_cust_type.deleted_flag
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.base.custtype.domain.vo.BaseCustTypeVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_base_cust_type
        <where>
            t_base_cust_type.deleted_flag = 0
        </where>
    </select>

    <select id="getDetailById"
            resultType="net.lab1024.sa.admin.module.business.base.custtype.domain.vo.BaseCustTypeVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_base_cust_type
        WHERE t_base_cust_type.cust_type_id = #{custTypeId}
        AND t_base_cust_type.deleted_flag = 0
    </select>
    <select id="selectByKeywords"
            resultType="net.lab1024.sa.admin.module.business.base.custtype.domain.vo.BaseCustTypeVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_base_cust_type
        WHERE t_base_cust_type.deleted_flag = 0
        <if test="keywords != null and keywords != ''">
            AND (t_base_cust_type.cust_type_name LIKE CONCAT('%', #{keywords}, '%')
            OR t_base_cust_type.cust_type_code LIKE CONCAT('%', #{keywords}, '%'))
        </if>
    </select>


    <update id="batchUpdateDeleted">
        update t_base_cust_type set deleted_flag = #{deletedFlag}
        where cust_type_id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

    <update id="updateDeleted">
        update t_base_cust_type
        set deleted_flag = #{deletedFlag}
        where cust_type_id = #{custTypeId}
    </update>

</mapper>
