<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.base.color.dao.BaseColorDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        t_base_color.color_id,
        t_base_color.color_code,
        t_base_color.color_name,
        t_base_color.color_eng_name,
        t_base_color.disabled_flag,
        t_base_color.remark,
        t_base_color.company_id,
        t_base_color.version,
        t_base_color.create_user_id,
        t_base_color.create_user_name,
        t_base_color.create_time,
        t_base_color.update_user_id,
        t_base_color.update_user_name,
        t_base_color.update_time,
        t_base_color.deleted_flag
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.base.color.domain.vo.BaseColorVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_base_color t_base_color
        <where>
            t_base_color.deleted_flag = 0
            <if test="queryForm.keywords != null and queryForm.keywords != ''">
                AND (
                t_base_color.color_code LIKE concat('%',#{queryForm.keywords},'%')
                OR t_base_color.color_name LIKE concat('%',#{queryForm.keywords},'%')
                OR t_base_color.color_eng_name LIKE concat('%',#{queryForm.keywords},'%')
                )
            </if>
        </where>
    </select>

    <update id="batchUpdateDeleted">
        update t_base_color set deleted_flag = #{deletedFlag}
        where color_id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

    <update id="updateDeleted">
        update t_base_color
        set deleted_flag = #{deletedFlag}
        where color_id = #{colorId}
    </update>

</mapper>
